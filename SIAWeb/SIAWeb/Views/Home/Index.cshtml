
@model SIAWeb.Models.WebLinkModel
@{
    ViewBag.Title = "SAPD Web Home";
}
<div class="container">
    <div class="row">
        <div class="col-lg-12" style="padding-bottom: 1px">
            @Html.Raw(@ViewBag.User)
            <span style="font-size: x-small; color: #0099FF; text-align: center;">Your top link picks</span>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12  col-md-12 col-sm-12 col-xs-12" style="padding-bottom: 1px">
            
            <div class="panel-body">
                
                    @foreach (var T in Model.TopLinks)
                    {
                    <a href=" @T.toplinkpath " class="btn btn-info btn-sm" target="_blank" style="width: 200px; margin-bottom: 6px;">@T.toplink
                    </a>
                        
                    }
                &nbsp;
            </div>
        </div>
    </div>
    
    <div class="row">
        <div class="col-lg-5 col-lg-offset-1 col-md-6 col-sm-6 col-xs-12">
            <div id="accordion" class="panel-group">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <i class="fa fa-angle-double-down"></i><a href="#panelBodyOne" data-toggle="collapse"
                                data-parent="#accordion">Daily Activity Center </a>
                        </h4>
                    </div>
                    <div id="panelBodyOne" class="panel-collapse collapse">
                        <div class="panel-body">
                            <ul>
                                @*@foreach (var item in Model)
                                {
                                    if (item.WebCategoryID == 2)
                                    {
                                    <li><a href=" @item.WebLink " target="_blank">@item.Name </a></li>
                                    }
                                }*@
                                @foreach (var link in Model.WebLinks)
                                {
                                    if (@link.category == 2)
                                    {
                                    <li><a href=" @link.linkPath " target="_blank">@link.link </a></li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>
                </div>
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <i class="fa fa-angle-double-down"></i><a href="#panelBodyTwo" data-toggle="collapse"
                                data-parent="#accordion">Management Systems </a>
                        </h4>
                    </div>
                    <div id="panelBodyTwo" class="panel-collapse collapse">
                        <div class="panel-body">
                            <ul>
                               @* @foreach (var item in Model)
                                {
                                    if (item.WebCategoryID == 1)
                                    {
                                    <li><a href=" @item.WebLink " target="_blank">@item.Name </a></li>
                                    }
                                }*@
                                @foreach (var link in Model.WebLinks)
                                {
                                    if (@link.category == 1)
                                    {
                                    <li><a href=" @link.linkPath " target="_blank">@link.link </a></li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>
                </div>
                 @*<div class="panel panel-primary">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <i class="fa fa-angle-double-down"></i><a href="#panelBodyThree" data-toggle="collapse"
                                data-parent="#accordion">Collective Bargaining </a>
                        </h4>
                    </div>
                    <div id="panelBodyThree" class="panel-collapse collapse">
                        <div class="panel-body">
                            <ul>
                                @foreach (var item in Model)
                                {
                                    if (item.WebCategoryID == 1)
                                    {
                                    <li><a href=" @item.WebLink " target="_blank">@item.Name </a></li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>
                </div>*@
            </div>
        </div>
        <div class="col-lg-5 col-md-6 col-sm-6 col-xs-12">
            <div id="accordion" class="panel-group">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <i class="fa fa-angle-double-down"></i><a href="#panelBodyFour" data-toggle="collapse"
                                data-parent="#accordion">Information/Criminal Search </a>
                        </h4>
                    </div>
                    <div id="panelBodyFour" class="panel-collapse collapse">
                        <div class="panel-body">
                            <ul>
                                @*@foreach (var item in Model)
                                {
                                    if (item.WebCategoryID == 6)
                                    {
                                    <li><a href=" @item.WebLink " target="_blank">@item.Name</a></li>
                                    }
                                }*@
                                @foreach (var link in Model.WebLinks)
                                {
                                    if (@link.category == 6)
                                    {
                                    <li><a href=" @link.linkPath " target="_blank">@link.link </a></li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>
                </div>
                @*<div class="panel panel-primary">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <span class="glyphicon glyphicon-menu-down"></span>
                            <a href="#panelBodyFive" data-toggle="collapse" data-parent="#accordion">Europe </a>
                        </h4>
                    </div>
                    <div id="panelBodyFive" class="panel-collapse collapse">
                        <div class="panel-body">
                            <ul>
                                <li>United Kingdom</li>
                                <li>France</li>
                                <li>Germany</li>
                            </ul>
                        </div>
                    </div>
                </div>*@
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <i class="fa fa-angle-double-down"></i><a href="#panelBodySix" data-toggle="collapse"
                                data-parent="#accordion">SAPD Links </a>
                        </h4>
                    </div>
                    <div id="panelBodySix" class="panel-collapse collapse">
                        <div class="panel-body">
                            <ul>
                                @*@foreach (var item in Model)
                                {
                                    if (item.WebCategoryID == 5)
                                    {
                                    <li><a href=" @item.WebLink " target="_blank">@item.Name</a></li>
                                    }
                                }*@
                                @foreach (var link in Model.WebLinks)
                                {
                                    if (@link.category == 5)
                                    {
                                    <li><a href=" @link.linkPath " target="_blank">@link.link </a></li>
                                    }
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section scripts
{
    <script type="text/javascript">
        $(document).ready(function () {

            $('.collapse').on('shown.bs.collapse', function () {
                $(this).parent().find('.fa-angle-double-down')
                                .removeClass('fa-angle-double-down').slideDown("3000")
                                .addClass('fa-angle-double-up');
            }).on('hidden.bs.collapse', function () {
                $(this).parent().find(".fa-angle-double-up")
                                .removeClass("fa-angle-double-up").slideDown("3000")
                                .addClass("fa-angle-double-down");
            });




        });

        $(function () {
            $('div.panel-body a').click(function () {
                var link = $(this).text()
                var page = "Home"
                $.ajax({
                    type: 'get',
                    url: '@Url.Action("updateStats")',
                    data: { "link": link, "page": page},
                    success: function (result) {

                    }
                });

            });
        });
     
    </script>
}
