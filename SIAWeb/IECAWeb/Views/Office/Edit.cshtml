@model IECAWeb.Models.IECA_AuditOffice

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>IECA_AuditOffice</legend>

        @Html.HiddenFor(model => model.AuditOfficeID)

        <div class="editor-label">
            @Html.LabelFor(model => model.VisibleFlag)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.VisibleFlag)
            @Html.ValidationMessageFor(model => model.VisibleFlag)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
